rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for smooth development, testing, and playtesting in AI Studio
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
